apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway
spec:
  gatewayClassName: cloud-provider-kind
  listeners:
    - name: default
      hostname: "*.local"
      port: 3435
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: All